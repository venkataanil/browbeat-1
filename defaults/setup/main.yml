---

browbeat_git_repo: "https://github.com/openstack/browbeat"
browbeat_git_revision: "HEAD"
browbeat_dependencies:
    - "ansible"
    - "python-pip"
browbeat_config: "browbeat-config.yaml"

install_shaker: False
